<Window x:Class="GettingReal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:GettingReal.View" xmlns:gettingreal="clr-namespace:GettingReal" d:DataContext="{d:DesignInstance Type=gettingreal:MainViewModel}"
        mc:Ignorable="d" 
        Title="MainWindow" Height="450" Width="800"
        Background="Transparent">

    <!--Window Highlight-->
    <Border CornerRadius="10" BorderThickness="2">
        <Border.BorderBrush>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0" >
                <GradientStop Color="{StaticResource WindowHightlightColor1}" Offset="0.9"/>
                <GradientStop Color="{StaticResource WindowHightlightColor2}" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Top Menu Section-->
            <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2">
                <Border CornerRadius="10,10,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0.5,1" >
                            <GradientStop Color="{StaticResource TopPanel1}" Offset="0"/>
                            <GradientStop Color="{StaticResource TopPanel2}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <!-- Buttons for switching views -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10">
                        <Button Content="Inventory" Command="{Binding ShowInventoryCommand}" Style="{StaticResource NavButtonStyle}" Click="Button_Click_1" Height="31" Width="80" />
                        <Button Content="Packaging" Command="{Binding ShowPackagingCommand}" Style="{StaticResource NavButtonStyle}" Click="Button_Click" Height="31" Width="85" />
                    </StackPanel>
                </Border>
            </Border>

            <!--Left Menu Section-->
            <Border Grid.Column="0" Grid.Row="1">
                <Border CornerRadius="0,0,0,10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0.4,1" >
                            <GradientStop Color="{StaticResource LeftPanel1}" Offset="0"/>
                            <GradientStop Color="{StaticResource LeftPanel2}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
            </Border>

            <!--Right Menu Section-->
            <Border Grid.Column="1" Grid.Row="1">
                <Border CornerRadius="0,0,10,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0.8,1" >
                            <GradientStop Color="{StaticResource RightPanel1}" Offset="0"/>
                            <GradientStop Color="{StaticResource RightPanel2}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
            </Border>

            <!-- Content Section -->
            <ContentControl Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Content="{Binding CurrentView}" />
        </Grid>
    </Border>
</Window>